# Notion Dashboard Contract

Notion is a readable projection, not the source of truth; Git YAML and Markdown
remain authoritative. Store the target Notion page or database identifier in a
project-local connector configuration that is excluded from Git when sensitive.
