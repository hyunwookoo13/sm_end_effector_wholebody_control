# Project Research OS

This directory is the traceable research record for `ROS 2 Research Project` and is
versioned independently inside its development repository.

Start with `PROJECT.yaml`, `research/00_constitution/`, the active Requirement,
`research/05_progress/current_status.yaml`, and the current Task brief. Run
`python3 scripts/validate_research_os.py` after meaningful changes.
